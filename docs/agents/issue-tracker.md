# Issue Tracker

This repository uses GitHub Issues for `Fei2-Labs/model-checker`.

Use `gh issue` commands when a skill needs to create, inspect, or update an issue.
Keep issue titles short and use the repo's normal GitHub workflow for triage and follow-up.
